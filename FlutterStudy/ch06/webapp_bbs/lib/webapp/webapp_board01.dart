import "package:flutter/material.dart";

class WebAppBoard extends StatelessWidget {
  const WebAppBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text("WebApp Board")
      ),
    );
  }
}
