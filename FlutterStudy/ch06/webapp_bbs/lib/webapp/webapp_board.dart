import "package:flutter/material.dart";

class WebAppBoard extends StatelessWidget {
  const WebAppBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("WebApp Board"),
    );
  }
}
